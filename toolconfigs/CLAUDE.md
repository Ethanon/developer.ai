<!--
  Installed at the target repo root as CLAUDE.md, which Claude Code reads automatically.
  A pointer, not a copy: the rules live in AGENTS.md so every tool reads the same text.
-->

# Project Anchor

**Read [`AGENTS.md`](AGENTS.md) first.** It holds the routing table, the Prime Directive, and
the headline rules for this repo. Everything below is Claude Code specific.

The agent specs live in `.claude/agents/`. Invoke one by name, or ask for what it does:
"security review this PR", "scan for dead code", "groom the backlog".

Reports land in `.claude/reports/`.
