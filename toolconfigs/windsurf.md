---
trigger: always_on
---

Read `AGENTS.md` at the repo root before any change, and the document under
`docs/engineering/` that covers what you are about to do.

The Prime Directive: the preferred number of lines of code is zero.

Non-negotiable: fail loud and never fabricate; default to zero comments; no timeout or
interval literals outside the config module; identity comes from the session; names never
leak a technology; tests are deterministic, offline, and fast.
